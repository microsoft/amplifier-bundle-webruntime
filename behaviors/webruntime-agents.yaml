bundle:
  name: webruntime-agents
  version: 0.1.0
  description: Web runtime agents for building Amplifier web apps

# NOTE: Context loads in the AGENT via @mentions, not here.
# This prevents root session from having enough knowledge to bypass delegation.

agents:
  include:
    - webruntime:webruntime-developer
