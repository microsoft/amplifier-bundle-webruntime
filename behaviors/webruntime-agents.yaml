behavior:
  name: webruntime-agents
  version: 0.1.0
  description: Web runtime agents for building Amplifier web apps

# NOTE: Do NOT include webruntime-context here!
# Context goes in the AGENT, not the root session.
# Root session should delegate, not do the work.

agents:
  include:
    - webruntime:agents/webruntime-developer
